apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: local-platform
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/<SENIN_REPO>/k8s-gitops
    targetRevision: main
    path: apps
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
